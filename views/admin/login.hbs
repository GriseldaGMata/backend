<!DOCTYPE html>
<html>
<div class="container">
  <div class="row" style="margin: 200px 0">
    <div class="col">
      <form action="/admin/login" method="post">
        <div class="form-group">
          <input
            type="text"
            class="form-control"
            placeholder="Usuario"
            name="usuario"
          />
        </div>
        <div class="form-group">
          <input
            type="password"
            class="form-control"
            placeholder="Password"
            name="password"
          />
        </div>

        <button type="submit" class="btn btn-outline-dark btn-lg">Ingresar</button>
      </form>
      {{#if error}}
      <div class="alert alert-danger d-flex align-items-center" role="alert">
      <svg class="bi flex-shrink-0 me-2" width="24" height="24" role="img" aria-label="Danger:"><use xlink:href="#exclamation-triangle-fill"/></svg>
      <div>  Usuario o clave incorrectos, por favor verifique los datos  </div>
      </div>
      {{/if}}
    </div>
  </div>
</div>
</html>